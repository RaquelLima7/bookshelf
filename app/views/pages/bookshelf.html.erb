<div class="container mt-5">
  <table>
    <thead>
      <tr>
        <th>Na estante</th>
        <th>Lendo</th>
        <th>Lido</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <% @books.each do |book| %>
            <div class="card">
              <div class="card-body">
                <h5 class="card-title"><%= book.title %></h5>
                <p class="card-text"> <%= book.author %></p>
                <p class="card-text"><%= book.description.truncate(100, omission: '...') %></p>
              </div>
            </div>
          <% end %>
        </td>
        <td>104(230)</td>
        <td>156.8 (346)</td>
      </tr>
      <tr>
        <td>Somali Ostrich</td>
        <td>90 (200)</td>
        <td>130 (287)</td>
    </tr>
    </tbody>
  </table>
</div>
